spring:
  application:
    name: ai-wealth-platform

# SpringDoc & Swagger UI 配置（生产环境禁用）
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

# 生产环境默认关闭 Knife4j，避免无意暴露接口结构和调试入口。
knife4j:
  enable: false
