-- ============================================================================
-- V30 扩展数据源采集频率字段
-- 说明：
--   数据源发现改为由远程大模型整理候选后，fetch_frequency 仍用于保存
--   cron 表达式或短频率枚举/短语。字段适度扩展到 255，防止外部模型输出
--   较长但仍可展示的频率说明时导致候选落库失败；采集计划长文本继续保存
--   在 discovery candidate/description/quality policy 等治理字段中。
-- ============================================================================

ALTER TABLE aiw_data_source
    MODIFY COLUMN fetch_frequency VARCHAR(255) NULL COMMENT '采集频率说明、短枚举或cron表达式';
