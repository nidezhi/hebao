-- ============================================================================
-- V31 扩展数据源维护方字段
-- 说明：
--   AI 数据源发现会返回维护机构、授权主体、官网团队等说明。owner 仍作为
--   短展示字段使用，但从 64 扩展到 255，避免可信来源名称较长时落库失败。
--   更长的采集计划和治理说明继续放入 description/任务摘要/候选详情。
-- ============================================================================

ALTER TABLE aiw_data_source
    MODIFY COLUMN owner VARCHAR(255) NULL COMMENT '数据源负责人、维护方或授权主体';
